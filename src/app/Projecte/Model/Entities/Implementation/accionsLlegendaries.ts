export class AccionsLlegendaries {
    nom: string;
    tipus: string;
    subtipus: string;

    constructor(nom: string, tipus: string, subtipus: string) {
        this.nom = nom;
        this.tipus = tipus;
        this.subtipus = subtipus;
    }
}